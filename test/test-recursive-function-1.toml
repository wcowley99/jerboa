name = "recursive function does not trample registers"

[input]
program = """
fn fib(n) {
  if n == 1 then { 
    1 
  } else {
    if n == 0 then {
      0
    } else {
      fib(n-1) + fib(n-2)
    }
  }
}

fib(10)
"""

[expected]
exit_code = 55
